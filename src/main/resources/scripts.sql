select * from student where age > 10 and age < 20;

select student.name from student;

select student.name like '%0%' from student;

select * from student where age < id;

select * from student order by age;
